#list is mutable 
#list can store different types of data
#list is ordered collection of data
# difference between list and set is list can store duplicate values but set cannot store duplicate values
# list is defined by using square brackets []
# list is indexed and ordered collection of data
# list is mutable which means we can change the values of list after creating it
# list can store different types of data like int, float, string, etc.
# list can be nested which means we can store list inside another list
# list can be sliced which means we can extract a portion of the list
# list can be concatenated which means we can combine two or more lists
# list can be repeated which means we can repeat the list multiple times
# list can be iterated which means we can loop through the list
# list can be sorted which means we can sort the list in ascending or descending order
# list can be reversed which means we can reverse the order of the list
# list can be copied which means we can create a copy of the list
# list can be cleared which means we can remove all the elements from the list
# list can be checked for membership which means we can check if an element is present in the list or not
# list can be counted which means we can count the number of occurrences of an element in the list
# list can be indexed which means we can access the elements of the list using their index 